{
  "name": "data-catalog-v01",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "build": "bun build server.ts --outdir ./dist --target bun",
    "start": "bun run server.ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
